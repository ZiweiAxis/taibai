# 天枢 API 根（发现、注册、心跳）
TIANSHU_API_BASE=http://localhost:8080
# 谛听审计上报 URL
DITING_AUDIT_URL=http://localhost:9090/api/audit
# 可选：已存在的 agent_id，用于仅验证心跳与上报
# VERIFICATION_AGENT_ID=agent-xxx
# 可选：注册时使用的 owner_id
# VERIFICATION_OWNER_ID=verification-owner
